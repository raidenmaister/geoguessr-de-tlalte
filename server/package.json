{
  "name": "geoguesser-server",
  "version": "1.0.0",
  "description": "Servidor multijugador para GeoGuessr Explorer — Express + Socket.io",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "socket.io": "^4.8.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
